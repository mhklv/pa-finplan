module finplan.client {
    requires javafx.controls;
    requires javafx.fxml;
    requires javafx.web;
    // requires junit;
    // requires org.apache.commons.codec.binary;
    requires poi;
    requires poi.ooxml;
    requires finplan.common;
    // requires commons.codec;

    // opens org.openjfx to javafx.fxml;
    // exports org.openjfx;
}
